import { ComingSoon } from "@/components/ComingSoon";

export default function DashboardPage() {
  return (
    <ComingSoon helper="Your memory upload dashboard is being built. Your contract is complete — we'll let you know when uploading is ready." />
  );
}
